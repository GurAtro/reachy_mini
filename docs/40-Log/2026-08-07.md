---
type: log
date: 2026-08-07
tasks:
  - "[[T-005 Claude API 실호출 검증]]"
---

# 2026-08-07

## 오늘 한 일
- 로컬 `~/orca/projects/Reachy`를 `GurAtro/reachy_mini`에 연결. 원격 기본 브랜치가 `master`라 로컬도 `master`로 맞췄고, 내용 없던 `main`은 삭제.
- `docs/`를 Obsidian 볼트로 구성. 모듈 8 · 작업 5 · 결정 3 · 참고 5장. Dataview 0.5.68 설치.
- README에 흩어져 있던 내용을 노트로 옮기면서 링크로 이었음 — VRAM 표, 데몬 충돌, 구동 절차, 동작 특성.
- `_scripts/sync_log.py` 작성. 2026-08-05 커밋 4개로 동작 확인, 재실행 멱등성 확인.

## 알게 된 것
- **README에 이미 값진 내용이 많았지만 "왜"가 본문에 섞여 있었다.** VRAM 때문에 Claude가 기본값이라는 판단, edge-tts의 프라이버시 대가, mock 폴백의 위험 — 셋 다 결정 노트로 떼어냈다. 나중에 뒤집을 때 근거를 다시 찾지 않아도 된다.
- **`shake`/`happy`/`confused`가 구현돼 있는데 아무도 호출하지 않는다.** 코드를 읽어야만 보이는 사실이라 [[T-001 로봇 모션을 LLM 도구로 노출]]로 명시해 뒀다.
- **`execute_tool(name, args)`가 dict만 받아서 로봇 핸들을 넘길 경로가 없다.** T-001의 진짜 난관은 스키마 추가가 아니라 여기다.
- 저장소가 public이다. 노트에 키나 개인 메모를 넣지 않도록 [[볼트 사용법]]에 못 박았다.

## 다음
- [ ] [[T-005 Claude API 실호출 검증]] — 다른 무엇보다 먼저. 기본 백엔드가 미검증 상태다.
- [ ] 검증 결과에 따라 [[M-llm 언어모델]]의 `status`와 README 경고 문단 정리

## 커밋
<!-- commits:start -->
- `3819659` docs: add Obsidian vault for design notes, tasks, and history (이재혁) — `.gitignore`
<!-- commits:end -->
