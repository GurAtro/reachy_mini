# ── STT ──────────────────────────────────────────────────────────────
faster-whisper        # Korean STT; runs on CUDA or CPU

# ── Audio I/O ────────────────────────────────────────────────────────
pyaudio               # PC microphone (audio.source: local)
sounddevice           # PC speaker playback
soundfile             # decode edge-tts / pyttsx3 output
numpy
scipy                 # resampling between TTS and device sample rates

# ── TTS ──────────────────────────────────────────────────────────────
edge-tts              # tts.backend: edge - free, natural Korean, needs internet
pyttsx3               # offline fallback for the edge backend (Windows SAPI)

# tts.backend: qwen - local Qwen3-TTS with voice cloning. Left commented out
# because it pulls in torch (~2.5 GB); install only if you use this backend:
#   pip install -U qwen-tts
#   pip install -U flash-attn --no-build-isolation   # optional, faster
# qwen-tts

# ── LLM backends ─────────────────────────────────────────────────────
anthropic>=0.100.0    # llm.backend: claude
ollama                # llm.backend: ollama

# ── Robot ────────────────────────────────────────────────────────────
# Reachy Mini SDK. NOT `reachy-sdk`, which targets the full-size Reachy 2.
# Only needed when reachy.enabled is true.
reachy-mini

# ── PC control tools ─────────────────────────────────────────────────
psutil
pyautogui

# ── Utilities ────────────────────────────────────────────────────────
pyyaml
requests
